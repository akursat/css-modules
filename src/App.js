import React from "react";
import Button from "./components/Button/Button";
import "./app.scss";

export default function App() {
  return (
      <div className="welcome">
      <h1>CSS MODULES + SCSS</h1>
      <p>CSS Modules with SCSS configuration</p>
      <Button/>
    </div>
  );
}
